# TODO: This chart was added for initial testing and simplicity.
# Use ../helm as the canonical chart. This directory will be removed.
apiVersion: v2
name: prl-devops-service
description: Parallels DevOps Service — lightweight chart for DI AKS (orchestrator / catalog)
type: application
version: "1.0.0"
appVersion: "1.0.0"
